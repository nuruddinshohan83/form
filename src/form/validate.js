function validate(value) {
    let error = { a: 1, b: 1 }
    //console.log(Object.entries(error).forEach([key,val])=>console.log(key,val))
    return error
}
validate({ username: "shohan" })
//export default
